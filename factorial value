read ano and print factorial value of given number 

#include <stdio.h>
int fact(int n)
{
    if(n==0)
    return 0;
    else
    n*fact(n-1);
}
    int main(){
    int n;
    scanf("%d",&n);
    int r= *(n);
    printf("%d",r);
    







    

 return 0;
}
