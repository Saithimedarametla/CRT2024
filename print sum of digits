read a no. and print sum of digits

#include <stdio.h>
int n
{
    if(n==0)
    return 0;
    else
    return % 10+ sum of digits (n/10);
}
int main()
{
    int n;
    scanf("%d",&n);
    int r=sum of digits (n);
    printf("%d",r);


return 0;
}
